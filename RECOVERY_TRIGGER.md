# Temporary recovery trigger

This file exists only to trigger the one-shot GitHub Actions recovery workflow that reconstructs and verifies the archived v0.16.0 baseline before restoring editable root source. Remove it after recovery is merged.
